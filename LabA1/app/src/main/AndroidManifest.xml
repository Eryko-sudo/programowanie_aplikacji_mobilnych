<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LabA1"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>


<!--<application>: Określa nazwę aplikacji, uprawnienia i inne ogólne konfiguracje.-->
<!--<activity>: Definiuje pojedynczą aktywność w aplikacji. W tej sekcji należy podać nazwę klasy aktywności, jej layout oraz filtry intencji.-->
<!--    <intent-filter>: Określa, jak użytkownik może uruchomić daną aktywność. Filtry intencji mogą zawierać informacje o akcji, kategorii danych i schemacie adresu URL.-->
